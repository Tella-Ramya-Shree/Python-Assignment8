#1.Program to get a single string from two given strings, separated by a space and swap the first two characters of each string
def swap(a, b):
  new_a = b[:2] + a[2:]
  new_b = a[:2] + b[2:]
  return new_a + ' ' + new_b
print(swap('abc', 'xyz'))

#2.Program to remove a newline in Python
str1='PYTHON\n'
print(str1)
print(str1.rstrip())

#3.Program to perform Deletion of a character 
str = "Engineering"
print ("Original string: " + str) 
res_str = str[1:]
print ("String after removal of character:",res_str)

#Program to print every character of a string entered by user in a new line using loop
s='mango'
for index in s:
  print(index)


